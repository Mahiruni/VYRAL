import { saveCreatorProfile } from './actions'
import { requireUser } from '@/lib/auth'
export default async function Onboarding({searchParams}:{searchParams:Promise<{error?:string}>}){const {user}=await requireUser();const p=await searchParams;return <main className="min-h-screen grid-bg grid place-items-center p-6"><div className="glass w-full max-w-lg rounded-2xl p-7"><p className="eyebrow">STEP 1 OF 2</p><h1 className="mt-2 font-display text-3xl font-bold">Build your Creator DNA</h1><p className="mt-2 text-sm leading-6 text-zinc-500">These inputs become the foundation for personalized recommendations. VYRAL will learn the rest from your actual content performance.</p>{p.error&&<div className="mt-5 rounded-xl bg-rose-500/10 p-3 text-sm text-rose-300">{p.error}</div>}<form action={saveCreatorProfile} className="mt-6 space-y-4"><input className="field" name="display_name" placeholder="Creator name" defaultValue={String(user.user_metadata?.display_name||'')}/><input className="field" name="niche" required placeholder="Primary niche (e.g. business, comedy, fitness)"/><input className="field" name="follower_count" type="number" min="0" placeholder="Current TikTok followers"/><button className="w-full rounded-xl bg-rose-500 py-3 font-bold hover:bg-rose-400">Save & continue</button></form></div></main>}
