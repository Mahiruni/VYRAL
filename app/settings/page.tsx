import Link from 'next/link'
import { requireUser } from '@/lib/auth'
import { PageShell } from '@/app/components/page-shell'
import { signOut } from '@/app/auth/actions'
export default async function Settings(){const {supabase,user}=await requireUser();const {data:account}=await supabase.from('tiktok_accounts').select('username,display_name,scopes,created_at').eq('user_id',user.id).maybeSingle();return <PageShell title="Settings" description="Control your account and data connections. VYRAL does not display fabricated integrations or metrics."><div className="grid gap-5 lg:grid-cols-2"><section className="glass rounded-2xl p-6"><p className="eyebrow">ACCOUNT</p><h2 className="mt-2 font-display text-xl font-bold">{user.email}</h2><p className="mt-2 text-sm text-zinc-500">Authenticated with Supabase.</p><Link href="/onboarding" className="mt-5 inline-flex rounded-xl border border-white/10 px-4 py-2.5 text-sm font-bold hover:bg-white/5">Edit Creator DNA</Link><form action={signOut} className="mt-3"><button className="text-sm text-rose-300 hover:text-rose-200">Sign out</button></form></section><section className="glass rounded-2xl p-6"><p className="eyebrow">TIKTOK CONNECTION</p>{account?<><h2 className="mt-2 font-display text-xl font-bold">@{account.username || account.display_name || 'Connected creator'}</h2><p className="mt-2 text-sm text-zinc-500">Permissions: {(account.scopes||[]).join(', ')||'none recorded'}</p></>:<><h2 className="mt-2 font-display text-xl font-bold">Not connected</h2><p className="mt-2 text-sm leading-6 text-zinc-500">TikTok OAuth is intentionally not simulated. Add approved TikTok app credentials before connecting an account.</p><button disabled className="mt-5 rounded-xl bg-white/5 px-4 py-2.5 text-sm font-bold text-zinc-600">Connect TikTok — configuration required</button></>}</section></div></PageShell>}
